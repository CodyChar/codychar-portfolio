export const metadata = {
  title: "Bonsai Fishtank — Head-Coupled Perspective",
  description: "Interactive 3D diorama with head tracking. Built with react-three-fiber.",
};

export default function BonsaiLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
